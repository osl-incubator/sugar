name: sugar
channels:
  - nodefaults
  - conda-forge
dependencies:
  - python
  - poetry >=1.5
  - nodejs >=18.17  # used by semantic-release
  - pip:
    # fix the distlib issue
    - paginate >=0.5
